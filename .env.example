# Written so the builder's output is owned by you rather than by root.
# `docker compose up` reads .env automatically; create it with:
#   printf 'UID=%s\nGID=%s\n' "$(id -u)" "$(id -g)" > .env
UID=1000
GID=1000
